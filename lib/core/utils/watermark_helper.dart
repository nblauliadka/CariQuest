// lib/core/utils/watermark_helper.dart
// Stub — web MVP does not require watermarking

class WatermarkHelper {
  /// Stub: returns original bytes unchanged in demo mode.
  static Future<List<int>> applyWatermark(List<int> imageBytes, String text) async {
    return imageBytes; // no-op in demo mode
  }
}
